#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../config/environment"
require "telegram/bot"

token_raw = ENV["TELEGRAM_BOT_TOKEN"].to_s
token = token_raw.strip[/\d+:[A-Za-z0-9_-]+/]
abort "Falta TELEGRAM_BOT_TOKEN (pegá solo el token tipo 123:ABC...)" if token.blank?

HELP_TEXT = <<~TXT.strip
  🧾 Bot de gastos e ingresos (ARS + dólar oficial BNA)

  Registrar (texto o foto de comprobante):
  • hamburguesa 8500
  • almohadillas 2500 por transferencia de mercadopago
  • netflix 8 usd
  • cobro 84150 servicio pilates
  • transferencia 21mil de gime
  • 2 menús 9000 cada uno

  Si un monto se ve raro (hamburguesa a $125.000, Netflix carísimo), te pregunto antes de guardar.

  Consultas:
  • /resumen — mes actual (ingresos, gastos, balance)
  • /septiembre — un mes
  • /movimientos — últimos 10
  • /dolar — cotización oficial Banco Nación
  • /perfil — tu rubro de trabajo

  Otros:
  • /trabajo — cambiar a qué te dedicás
  • /borrarultimo — deshacer el último movimiento
  • /ayuda — esta ayuda
TXT

SPANISH_COMMANDS = [
  Telegram::Bot::Types::BotCommand.new(command: "inicio", description: "Empezar y configurar perfil"),
  Telegram::Bot::Types::BotCommand.new(command: "ayuda", description: "Ver comandos"),
  Telegram::Bot::Types::BotCommand.new(command: "resumen", description: "Resumen del mes actual"),
  Telegram::Bot::Types::BotCommand.new(command: "movimientos", description: "Últimos 10 movimientos"),
  Telegram::Bot::Types::BotCommand.new(command: "perfil", description: "Ver tu perfil"),
  Telegram::Bot::Types::BotCommand.new(command: "trabajo", description: "Cambiar a qué te dedicás"),
  Telegram::Bot::Types::BotCommand.new(command: "dolar", description: "Dólar oficial Banco Nación"),
  Telegram::Bot::Types::BotCommand.new(command: "borrarultimo", description: "Borrar el último movimiento"),
  Telegram::Bot::Types::BotCommand.new(command: "telefono", description: "Asociar tu número")
].freeze

CONFIRM_YES = "Sí, está bien"
CONFIRM_CANCEL = "Cancelar"

def ensure_user(message)
  telegram_user_id = message.from&.id&.to_s
  chat_id = message.chat&.id&.to_s
  raise "No pude leer telegram_user_id/chat_id" if telegram_user_id.blank? || chat_id.blank?

  user = User.find_or_create_by!(telegram_user_id: telegram_user_id)
  user.update!(telegram_chat_id: chat_id) if user.telegram_chat_id != chat_id
  user
end

def format_ars(amount_cents)
  MoneyFormat.ars(amount_cents)
end

def format_money(amount_cents, rate: BnaOfficialDollar.current)
  MoneyFormat.ars_with_usd(amount_cents, rate: rate)
end

def suggestion_label(cents)
  "Usar #{MoneyFormat.ars(cents)}"
end

def kind_label(kind)
  kind.to_s == "income" ? "Ingreso" : "Gasto"
end

def command_name(text)
  text.to_s.split(/\s+/, 2).first.to_s.sub(%r{\A/}, "").split("@", 2).first.downcase
end

def btn(label)
  Telegram::Bot::Types::KeyboardButton.new(text: label)
end

def occupation_keyboard
  Telegram::Bot::Types::ReplyKeyboardMarkup.new(
    keyboard: [
      [btn("Empleado"), btn("Docente")],
      [btn("Vendedor"), btn("Estudiante")],
      [btn("Otro")]
    ],
    resize_keyboard: true,
    one_time_keyboard: true
  )
end

def remove_keyboard
  Telegram::Bot::Types::ReplyKeyboardRemove.new(remove_keyboard: true)
end

def ask_occupation(bot, chat_id, intro: true)
  prefix = intro ? "Para categorizar mejor tus cobros y gastos, " : ""
  bot.api.send_message(
    chat_id: chat_id,
    text: "#{prefix}¿a qué te dedicás?",
    reply_markup: occupation_keyboard
  )
end

def send_profile(bot, chat_id, user)
  rubro = user.occupation.presence || "todavía no lo cargaste"
  rate = BnaOfficialDollar.current
  lines = [
    "👤 Perfil",
    "Rubro: #{rubro}",
    "Moneda: #{user.currency}",
    "Zona: #{user.time_zone}"
  ]
  lines << dollar_rate_line(rate) if rate
  bot.api.send_message(chat_id: chat_id, text: lines.join("\n"))
end

def dollar_rate_line(rate)
  return "💵 Oficial BNA no disponible ahora" unless rate

  stale = rate.stale ? " (última conocida)" : ""
  updated = rate.updated_at&.in_time_zone&.strftime("%d/%m %H:%M")
  "💵 Oficial BNA compra #{MoneyFormat.ars((rate.compra * 100).round)} / venta #{MoneyFormat.ars((rate.venta * 100).round)}#{stale}" \
    "#{updated ? " — #{updated}" : ""}"
end

def send_dollar_rate(bot, chat_id)
  rate = BnaOfficialDollar.current
  unless rate
    bot.api.send_message(chat_id: chat_id, text: "No pude leer el dólar oficial BNA ahora. Probá en un rato.")
    return
  end

  bot.api.send_message(chat_id: chat_id, text: dollar_rate_line(rate))
end

def resolve_amount_and_kind(text, extracted)
  amount_cents, description, kind, payment_method = ExpenseTextParser.parse(
    text,
    usd_venta: BnaOfficialDollar.current&.venta
  )
  if PaymentMethodDetector.outflow?(text) && !ExpenseTextParser.strong_income?(text)
    kind = "expense"
  elsif extracted&.kind.present?
    kind = extracted.kind
  end

  if text.match?(/cada\s+(uno|una)\b/i) && extracted&.amount_cents.to_i.positive?
    amount_cents = extracted.amount_cents
  end

  description = extracted.description if extracted&.description.present? && description.blank?
  description = PaymentMethodDetector.strip(description.to_s) if description.present?
  payment_method = extracted.payment_method if payment_method.blank? && extracted&.payment_method.present?
  [amount_cents, description, kind, payment_method]
end

def classify_entry(description, kind, extracted)
  if extracted&.category.present?
    ExpenseClassifier::Category.new(
      category: extracted.category,
      subcategory: extracted.subcategory,
      confidence: extracted.confidence
    )
  else
    ExpenseClassifier.new.classify(description, kind: kind)
  end
end

def send_month_summary(bot, chat_id, user, query_text)
  range = ExpenseMonthSummary.parse_month_query(query_text)
  if range.nil?
    bot.api.send_message(
      chat_id: chat_id,
      text: "No entendí el mes. Probá: /septiembre  /mes septiembre  /mes 9  /resumen"
    )
    return
  end

  from, to = range
  result = ExpenseMonthSummary.for_user(user, from: from, to: to)
  bot.api.send_message(
    chat_id: chat_id,
    text: ExpenseMonthSummary.format_message(
      result,
      format_ars: method(:format_ars),
      usd_rate: BnaOfficialDollar.current
    )
  )
end

def send_recent(bot, chat_id, user, limit: 10)
  expenses = user.expenses.order(spent_at: :desc).limit(limit)
  if expenses.empty?
    bot.api.send_message(chat_id: chat_id, text: "Todavía no tenés movimientos registrados.")
    return
  end

  lines = ["🧾 Últimos #{expenses.size} movimientos:"]
  expenses.each do |e|
    day = e.spent_at.in_time_zone.strftime("%d/%m")
    sign = e.income? ? "+" : "-"
    usd = e.metadata.is_a?(Hash) ? e.metadata.dig("bna", "usd") || e.metadata.dig(:bna, :usd) : nil
    usd_txt = usd ? " (#{MoneyFormat.usd(usd.to_f)})" : ""
    pay = e.payment_method.present? ? " · #{e.payment_method}" : ""
    lines << "• #{day} #{sign}#{format_ars(e.amount_cents)}#{usd_txt} — #{kind_label(e.kind)}/#{e.category}#{pay} — #{e.description}"
  end
  bot.api.send_message(chat_id: chat_id, text: lines.join("\n"))
end

def extract_for(user, text, ai)
  ai.extract(text, currency: user.currency, occupation: user.occupation)
end

def persist_movement(user, amount_cents:, description:, kind:, extracted:, raw_text:, metadata:, payment_method: nil)
  cat = classify_entry(description, kind, extracted)
  method = payment_method.presence || extracted&.payment_method.presence
  method = nil unless PaymentMethodDetector::LABELS.include?(method)
  user.expenses.create!(
    amount_cents: amount_cents,
    currency: user.currency,
    description: description,
    category: cat.category,
    subcategory: cat.subcategory,
    kind: kind,
    payment_method: method,
    spent_at: Time.zone.now,
    raw_text: raw_text,
    llm_provider: extracted&.provider,
    llm_model: extracted&.model,
    llm_confidence: cat.confidence,
    metadata: dollar_metadata(amount_cents, metadata)
  )
end

def dollar_metadata(amount_cents, extra)
  meta = (extra || {}).transform_keys(&:to_s)
  rate = BnaOfficialDollar.current
  if rate
    meta["bna"] = {
      "compra" => rate.compra,
      "venta" => rate.venta,
      "usd" => BnaOfficialDollar.ars_to_usd(amount_cents, rate: rate)&.round(4),
      "source" => rate.source,
      "at" => rate.updated_at&.iso8601,
      "stale" => rate.stale
    }
  end
  meta
end

def saved_message(expense, prefix: nil)
  head = [prefix, kind_label(expense.kind)].compact.join(" ")
  pay = expense.payment_method.present? ? " · #{expense.payment_method}" : ""
  "#{head}: #{format_money(expense.amount_cents)} — #{[expense.category, expense.subcategory].compact.join(" / ")}#{pay} — #{expense.description}"
end

def clear_open_drafts(user)
  user.draft_expenses.where(state: %w[awaiting_amount awaiting_confirmation]).delete_all
end

def confirmation_keyboard(suggestions)
  rows = [[btn(CONFIRM_YES)]]
  suggestions.each { |s| rows << [btn(suggestion_label(s.amount_cents))] }
  rows << [btn(CONFIRM_CANCEL)]
  Telegram::Bot::Types::ReplyKeyboardMarkup.new(
    keyboard: rows,
    resize_keyboard: true,
    one_time_keyboard: true
  )
end

def suspicious_message(check, amount_cents, description, rate)
  item = check.item_label.presence || description
  lines = ["🤔 ¿Está bien el monto?", ""]
  lines << "#{item.to_s.capitalize} a #{format_money(amount_cents, rate: rate)} se me hace caro."
  if check.min_usd && check.max_usd && rate&.venta.to_f.positive?
    min_ars = BnaOfficialDollar.usd_to_ars_cents(check.min_usd, rate: rate)
    max_ars = BnaOfficialDollar.usd_to_ars_cents(check.max_usd, rate: rate)
    lines << "Lo habitual anda entre #{MoneyFormat.usd(check.min_usd)} y #{MoneyFormat.usd(check.max_usd)} (unos #{format_ars(min_ars)} a #{format_ars(max_ars)})."
  end
  if check.suggestions.any?
    sug = check.suggestions.first
    lines << ""
    lines << "¿Era #{format_money(sug.amount_cents, rate: rate)}?"
  end
  lines.join("\n")
end

def save_or_confirm(bot, chat_id, user, amount_cents:, description:, kind:, extracted:, raw_text:, metadata:, prefix: nil, payment_method: nil)
  rate = BnaOfficialDollar.current
  cat = classify_entry(description, kind, extracted)
  method = payment_method.presence || extracted&.payment_method
  check = AmountSanityChecker.check(
    amount_cents: amount_cents,
    description: description,
    kind: kind,
    category: cat.category,
    user: user,
    rate: rate
  )

  if check.suspicious
    clear_open_drafts(user)
    user.draft_expenses.create!(
      raw_text: raw_text,
      state: "awaiting_confirmation",
      extracted: {
        "amount_cents" => amount_cents,
        "description" => description,
        "kind" => kind,
        "payment_method" => method,
        "category" => cat.category,
        "subcategory" => cat.subcategory,
        "confidence" => cat.confidence,
        "provider" => extracted&.provider,
        "model" => extracted&.model,
        "suggestions" => check.suggestions.map { |s| { "amount_cents" => s.amount_cents } },
        "metadata" => metadata,
        "prefix" => prefix
      }
    )
    bot.api.send_message(
      chat_id: chat_id,
      text: suspicious_message(check, amount_cents, description, rate),
      reply_markup: confirmation_keyboard(check.suggestions)
    )
    return
  end

  expense = persist_movement(
    user,
    amount_cents: amount_cents,
    description: description,
    kind: kind,
    extracted: extracted,
    raw_text: raw_text,
    metadata: metadata,
    payment_method: method
  )
  bot.api.send_message(
    chat_id: chat_id,
    text: saved_message(expense, prefix: prefix),
    reply_markup: remove_keyboard
  )
end

def handle_confirmation(bot, chat_id, user, text)
  draft = user.draft_expenses.order(created_at: :desc).find_by(state: "awaiting_confirmation")
  return :none unless draft

  data = draft.extracted || {}
  suggestions = Array(data["suggestions"])
  lowered = text.to_s.strip.downcase

  if text == CONFIRM_CANCEL || %w[cancelar no].include?(lowered)
    draft.destroy!
    bot.api.send_message(chat_id: chat_id, text: "Listo, no lo guardé.", reply_markup: remove_keyboard)
    return :handled
  end

  chosen = nil
  if text == CONFIRM_YES || lowered.match?(/\A(s[ií]|ok|dale|confirmo)\z/)
    chosen = data["amount_cents"].to_i
  else
    match = suggestions.find { |s| text == suggestion_label(s["amount_cents"].to_i) }
    chosen = match["amount_cents"].to_i if match
  end
  return :ignore unless chosen.to_i.positive?

  extracted = ExpenseAiExtractor::Extraction.new(
    amount_cents: chosen,
    description: data["description"],
    category: data["category"],
    subcategory: data["subcategory"],
    confidence: data["confidence"].to_f,
    provider: data["provider"],
    model: data["model"],
    kind: data["kind"],
    payment_method: data["payment_method"]
  )
  expense = persist_movement(
    user,
    amount_cents: chosen,
    description: data["description"],
    kind: data["kind"],
    extracted: extracted,
    raw_text: draft.raw_text,
    metadata: (data["metadata"] || {}).merge("confirmed" => true),
    payment_method: data["payment_method"]
  )
  draft.destroy!
  bot.api.send_message(
    chat_id: chat_id,
    text: saved_message(expense, prefix: data["prefix"]),
    reply_markup: remove_keyboard
  )
  :handled
end

def download_telegram_file(token, file_path, message_id)
  url = "https://api.telegram.org/file/bot#{token}/#{file_path}"
  dir = Rails.root.join("tmp/receipts")
  FileUtils.mkdir_p(dir)
  ext = File.extname(file_path.to_s)
  ext = ".jpg" if ext.blank?
  dest = dir.join("#{message_id}#{ext}")
  res = HTTPX.with(timeout: { connect_timeout: 10, read_timeout: 30 }).get(url.to_s)
  return nil if res.is_a?(HTTPX::ErrorResponse) || res.status.to_i != 200

  File.binwrite(dest, res.to_s)
  dest
end

def handle_photo(bot, token, message, user, ai)
  photo = Array(message.photo).max_by { |p| p.file_size.to_i }
  return bot.api.send_message(chat_id: message.chat.id, text: "No pude leer la foto.") unless photo

  file = bot.api.get_file(file_id: photo.file_id)
  path = download_telegram_file(token, file.file_path, message.message_id)
  unless path
    bot.api.send_message(chat_id: message.chat.id, text: "No pude descargar la foto. Probá de nuevo.")
    return
  end

  caption = message.caption.to_s.strip
  ocr_text = ReceiptOcr.extract_text(path)
  combined = [caption, ocr_text].compact_blank.join("\n")

  unless ReceiptOcr.available?
    user.draft_expenses.create!(
      raw_text: caption.presence || "comprobante foto",
      extracted: { "description" => caption.presence || "comprobante", "kind" => "expense", "source" => "photo" },
      state: "awaiting_amount"
    )
    bot.api.send_message(
      chat_id: message.chat.id,
      text: "Recibí la foto. Todavía no hay OCR en este servidor.\nEscribí el monto y de qué fue, ej: super 12500 o cobro 84150 pilates."
    )
    return
  end

  if combined.blank?
    user.draft_expenses.create!(
      raw_text: "comprobante foto",
      extracted: { "description" => "comprobante", "kind" => "expense", "source" => "photo" },
      state: "awaiting_amount"
    )
    bot.api.send_message(chat_id: message.chat.id, text: "Vi la foto pero no pude leer el texto. ¿Cuánto fue y de qué?")
    return
  end

  extracted = extract_for(user, combined, ai)
  amount_cents, description, kind, payment_method = resolve_amount_and_kind(combined, extracted)

  if amount_cents.nil?
    user.draft_expenses.create!(
      raw_text: combined,
      extracted: { "description" => description, "kind" => kind, "payment_method" => payment_method, "source" => "photo_ocr" },
      state: "awaiting_amount"
    )
    bot.api.send_message(
      chat_id: message.chat.id,
      text: "Leí el comprobante pero no encontré el monto.\nTexto: \"#{combined.truncate(180)}\"\n¿Cuánto fue en ARS?"
    )
    return
  end

  save_or_confirm(
    bot,
    message.chat.id,
    user,
    amount_cents: amount_cents,
    description: description,
    kind: kind,
    extracted: extracted,
    raw_text: combined,
    metadata: { flow: "photo_ocr" },
    prefix: "(foto)",
    payment_method: payment_method
  )
end

Telegram::Bot::Client.run(token) do |bot|
  ai = ExpenseAiExtractor.new
  bot.api.set_my_commands(commands: SPANISH_COMMANDS, language_code: "es") rescue nil

  bot.listen do |message|
    case message
    when Telegram::Bot::Types::Message
      next if message.text.blank? && message.contact.blank? && message.photo.blank?

      user = ensure_user(message)
      text = message.text.to_s.strip
      cmd = command_name(text)

      if message.photo.present? && !user.needs_onboarding?
        handle_photo(bot, token, message, user, ai)
        next
      end

      if user.needs_onboarding? && !%w[inicio start ayuda help].include?(cmd)
        if text.start_with?("/") && text.present?
          bot.api.send_message(chat_id: message.chat.id, text: "Primero decime a qué te dedicás (podés escribirlo, ej: soy programador).")
          ask_occupation(bot, message.chat.id, intro: false)
          next
        end

        if text.blank?
          ask_occupation(bot, message.chat.id, intro: false)
          next
        end

        if text == "Otro"
          user.update!(onboarding_step: "ask_occupation_detail")
          bot.api.send_message(chat_id: message.chat.id, text: "Contame en una frase a qué te dedicás.", reply_markup: remove_keyboard)
          next
        end

        occupation = User.normalize_occupation(text)
        if occupation.present?
          user.update!(occupation: occupation, onboarding_step: nil)
          bot.api.send_message(chat_id: message.chat.id, text: "Perfecto, rubro: #{occupation}.\n\n#{HELP_TEXT}", reply_markup: remove_keyboard)
          next
        end

        user.update!(onboarding_step: "ask_occupation") unless user.onboarding_step.present?
        ask_occupation(bot, message.chat.id)
        next
      end

      if %w[inicio start].include?(cmd)
        if user.onboarded?
          bot.api.send_message(chat_id: message.chat.id, text: "Hola de nuevo (#{user.occupation}).\n\n#{HELP_TEXT}")
        else
          user.update!(onboarding_step: "ask_occupation")
          ask_occupation(bot, message.chat.id)
        end
        next
      end

      if %w[ayuda help].include?(cmd)
        bot.api.send_message(chat_id: message.chat.id, text: HELP_TEXT)
        next
      end

      if %w[perfil].include?(cmd)
        send_profile(bot, message.chat.id, user)
        next
      end

      if %w[dolar dólar].include?(cmd)
        send_dollar_rate(bot, message.chat.id)
        next
      end

      if %w[trabajo].include?(cmd)
        user.update!(onboarding_step: "ask_occupation")
        ask_occupation(bot, message.chat.id, intro: false)
        next
      end

      if %w[movimientos ultimos últimos].include?(cmd)
        send_recent(bot, message.chat.id, user)
        next
      end

      if %w[borrarultimo borrar_ultimo deshacer].include?(cmd)
        last = user.expenses.order(created_at: :desc).first
        if last.nil?
          bot.api.send_message(chat_id: message.chat.id, text: "No hay nada para borrar.")
        else
          summary = "#{kind_label(last.kind)} #{format_ars(last.amount_cents)} — #{last.description}"
          last.destroy!
          bot.api.send_message(chat_id: message.chat.id, text: "Borré el último movimiento: #{summary}")
        end
        next
      end

      if %w[resumen mes].include?(cmd) || ExpenseMonthSummary::MONTHS.key?(cmd)
        query = text.sub(%r{\A/}, "").split("@", 2).first
        send_month_summary(bot, message.chat.id, user, query)
        next
      end

      if %w[telefono phone].include?(cmd)
        kb = Telegram::Bot::Types::ReplyKeyboardMarkup.new(
          keyboard: [[Telegram::Bot::Types::KeyboardButton.new(text: "Compartir mi contacto", request_contact: true)]],
          resize_keyboard: true,
          one_time_keyboard: true
        )
        bot.api.send_message(chat_id: message.chat.id, text: "Tocá para compartir tu número (opcional).", reply_markup: kb)
        next
      end

      if message.contact
        if message.contact.user_id.present? && message.contact.user_id.to_s != message.from&.id&.to_s
          bot.api.send_message(chat_id: message.chat.id, text: "Por seguridad, solo puedo guardar tu propio contacto.")
          next
        end

        phone = message.contact.phone_number.to_s
        phone = phone.start_with?("+") ? phone : "+#{phone}"
        user.update!(phone_e164: phone)
        bot.api.send_message(chat_id: message.chat.id, text: "OK. Teléfono asociado: #{phone}", reply_markup: remove_keyboard)
        next
      end

      if text.start_with?("/")
        bot.api.send_message(chat_id: message.chat.id, text: "Comando desconocido.\n\n#{HELP_TEXT}")
        next
      end

      confirmation = handle_confirmation(bot, message.chat.id, user, text)
      next if confirmation == :handled

      if confirmation == :ignore
        user.draft_expenses.where(state: "awaiting_confirmation").delete_all
      end

      draft = user.draft_expenses.order(created_at: :desc).find_by(state: "awaiting_amount")
      if draft && text.match?(/\d/)
        amount_cents, _, kind_from_amount, payment_from_amount = ExpenseTextParser.parse(
          text,
          currency: user.currency,
          usd_venta: BnaOfficialDollar.current&.venta
        )
        if amount_cents.nil?
          bot.api.send_message(chat_id: message.chat.id, text: "No entendí el monto. Probá: 8500, 23.000,50 o 8 usd")
          next
        end

        description = draft.extracted["description"].presence || draft.raw_text
        raw_text = draft.raw_text
        kind = draft.extracted["kind"].presence || kind_from_amount || "expense"
        payment_method = payment_from_amount.presence || draft.extracted["payment_method"]
        extracted = extract_for(user, description, ai)
        kind = extracted.kind if extracted.kind.present? && !PaymentMethodDetector.outflow?(raw_text)
        draft.destroy!
        save_or_confirm(
          bot,
          message.chat.id,
          user,
          amount_cents: amount_cents,
          description: description,
          kind: kind,
          extracted: extracted,
          raw_text: raw_text,
          metadata: { flow: "draft_amount" },
          payment_method: payment_method
        )
        next
      end

      extracted = extract_for(user, text, ai)
      amount_cents, description, kind, payment_method = resolve_amount_and_kind(text, extracted)

      if amount_cents.nil?
        user.draft_expenses.where(state: "awaiting_amount").delete_all
        user.draft_expenses.create!(
          raw_text: text,
          extracted: { "description" => description, "kind" => kind, "payment_method" => payment_method },
          state: "awaiting_amount"
        )
        bot.api.send_message(chat_id: message.chat.id, text: "¿Cuánto fue el monto en ARS para: \"#{description}\"? También sirve 8 usd.")
        next
      end

      save_or_confirm(
        bot,
        message.chat.id,
        user,
        amount_cents: amount_cents,
        description: description,
        kind: kind,
        extracted: extracted,
        raw_text: text,
        metadata: { flow: "direct", parser: { amount_cents: amount_cents, kind: kind, payment_method: payment_method } },
        payment_method: payment_method
      )
    end
  rescue StandardError => e
    bot.api.send_message(chat_id: message.chat.id, text: "Error: #{e.class} - #{e.message}") rescue nil
  end
end
